const endpoints = {
  // Tranaction end points 
  getTransactions: 'api/v1/transactions',
  postTranasctions: 'api/v1/transactions',

  // Items a endpoints 
  getItem : 'api/v1/items',
  postItem: 'api/v1/items'
};

export default endpoints;
